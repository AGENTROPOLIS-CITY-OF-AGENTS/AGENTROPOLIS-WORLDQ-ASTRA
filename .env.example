# AGENTROPOLIS WORLDQ front-end environment
# Production Grok Build gateway. Keep OPENAI_API_KEY on the gateway/server only.
VITE_GATEWAY_URL=https://your-grok-build-gateway.example.com

# Optional legacy WebSocket telemetry source. Leave unset when using the gateway SSE flow.
# VITE_AGENT_STREAM_URL=wss://your-agent-stream.example.com/events
